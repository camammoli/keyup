services:
  keyup:
    build: .
    ports:
      - "${WEB_PORT:-8080}:8080"
    volumes:
      - ./config.yml:/app/config.yml:ro
    restart: unless-stopped
    network_mode: host   # needed for UDP (Homebrew protocol)
